# План: Чипы загрузок в строке раздела

## Цель
Отображать в строке раздела (TimelineRow) суммарные загрузки всех этапов в виде чипов на таймлайне. Чипы должны показывать **команду** и **имя/фамилию сотрудника** (по аналогии с EmployeeRow, где показывается проект + этап).

---

## Текущее состояние

### TimelineRow (строка раздела)
- Показывает название раздела, аватар ответственного, счётчик загрузок, суммарную ставку
- При раскрытии → отображает StageRow и LoadingRow с детализацией
- **На таймлайне НЕТ чипов загрузок** — только цветные полосы активности этапов

### EmployeeRow (строка сотрудника)
- Использует `loading-bars-utils.ts` для расчёта баров
- Бары содержат: иконку `FolderKanban` + название проекта, иконку `Milestone` + название этапа
- Высота бара пропорциональна ставке (rate)
- Перекрывающиеся загрузки размещаются друг под другом (вертикальный стакинг)

---

## Архитектура решения

### 1. Новые утилиты в `loading-bars-utils.ts`

```typescript
// Новый интерфейс для периода загрузки в контексте раздела
export interface SectionLoadingPeriod {
  id: string
  type: "section_loading"
  startDate: Date
  endDate: Date
  rate: number
  // Данные сотрудника
  responsibleId: string
  responsibleName: string
  responsibleTeamName: string
  responsibleAvatarUrl?: string
  // Данные этапа (для группировки цветов)
  stageId: string
  stageName?: string
  // Исходная загрузка
  loading: Loading
}

// Преобразует загрузки раздела в периоды для отрисовки чипов
export function sectionLoadingsToPeriods(loadings: Loading[]): SectionLoadingPeriod[]

// Генерирует цвет на основе команды сотрудника (для различения чипов)
export function getTeamColor(teamName: string, isDark: boolean): string
```

### 2. Модификация TimelineRow

#### Где добавить чипы
В блоке `{/* Ячейки для каждого периода */}` — добавить overlay с барами загрузок (по аналогии с EmployeeRow).

#### Логика отображения
- Чипы отображаются **всегда** (и в свёрнутом, и в развёрнутом состоянии раздела)
- При развёрнутом состоянии — чипы дублируют информацию из LoadingRow, но в агрегированном виде

#### Структура чипа
```
┌─────────────────────────────────┐
│ 👥 Команда  │  Имя Фамилия     │
└─────────────────────────────────┘
```

Или вертикально (для узких баров):
```
┌─────────────┐
│ 👥 Команда  │
│ Имя Фамилия │
└─────────────┘
```

### 3. Высота строки раздела

Нужно динамически рассчитывать высоту `TimelineRow` на основе количества перекрывающихся загрузок (как в EmployeeRow).

---

## Детальный план изменений

### Шаг 1: Расширить `loading-bars-utils.ts`

**Файл:** `modules/planning/components/timeline/loading-bars-utils.ts`

1. Добавить интерфейс `SectionLoadingPeriod`
2. Добавить функцию `sectionLoadingsToPeriods(loadings: Loading[])`
3. Добавить функцию `getTeamColor(teamName: string, isDark: boolean)`
4. Добавить функцию `getSectionLoadingLabelParts(period: SectionLoadingPeriod, barWidth: number)` — аналог `getBarLabelParts`, но для команды + имени

### Шаг 2: Создать компонент SectionLoadingBars

**Новый файл:** `modules/planning/components/timeline/section-loading-bars.tsx`

```typescript
interface SectionLoadingBarsProps {
  loadings: Loading[]
  timeUnits: TimelineUnit[]
  cellWidth: number
  theme: string
  rowHeight: number
}

export function SectionLoadingBars({ ... }: SectionLoadingBarsProps) {
  // 1. Преобразовать loadings в SectionLoadingPeriod[]
  // 2. Рассчитать barRenders через calculateBarRenders
  // 3. Отрисовать бары с чипами (команда + имя)
}
```

### Шаг 3: Интегрировать в TimelineRow

**Файл:** `modules/planning/components/timeline/timeline-row.tsx`

1. Импортировать `SectionLoadingBars`
2. В блоке ячеек таймлайна добавить:
   ```tsx
   <SectionLoadingBars
     loadings={uniqueLoadings}
     timeUnits={timeUnits}
     cellWidth={cellWidth}
     theme={theme}
     rowHeight={rowHeight}
   />
   ```
3. Добавить динамический расчёт высоты строки на основе `getMaxLayers`

### Шаг 4: Адаптивное отображение чипов

В зависимости от ширины бара:
- **Широкий (>120px)**: Полный вид — иконка команды + название команды + имя фамилия
- **Средний (70-120px)**: Компактный — команда на одной строке, имя на другой
- **Узкий (35-70px)**: Минимальный — только имя (или инициалы)
- **Очень узкий (<35px)**: Только иконка `Users`

---

## Визуальный пример

### До (текущее состояние)
```
┌─────────────────────────────────────────────────────────────────┐
│ 📁 Название раздела  [3] [📊 1.5 ставки]  │░░░░░░░░░░░░░░░░░░░░│
└─────────────────────────────────────────────────────────────────┘
```
(На таймлайне только цветные полоски этапов)

### После (новое поведение)
```
┌─────────────────────────────────────────────────────────────────┐
│ 📁 Название раздела  [3]  │ ┌──────────────┐ ┌────────────────┐ │
│                           │ │👥 Отд.1      │ │👥 Отд.2        │ │
│                           │ │Иван Иванов   │ │Пётр Петров     │ │
│                           │ └──────────────┘ └────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## Типы данных

Из `Loading` используем:
```typescript
{
  responsibleId: string
  responsibleName: string           // "Иван Иванов"
  responsibleTeamName: string       // "Frontend Team"
  responsibleAvatarUrl?: string
  startDate: Date
  endDate: Date
  rate: number
  stageId: string                   // Для группировки цвета
}
```

---

## Оценка сложности

| Компонент | Сложность | Описание |
|-----------|-----------|----------|
| Утилиты в `loading-bars-utils.ts` | Низкая | Добавление новых функций по аналогии с существующими |
| `SectionLoadingBars` компонент | Средняя | Новый компонент, но логика аналогична EmployeeRow |
| Интеграция в TimelineRow | Низкая | Добавление компонента + расчёт высоты |
| Адаптивные чипы | Средняя | Логика адаптивного отображения |

---

## Зависимости

- Не требуется изменений в БД
- Не требуется изменений в API
- Все данные уже доступны в `Loading` (`responsibleName`, `responsibleTeamName`)

---

## Открытые вопросы

1. **Группировка по сотруднику**: Объединять ли смежные периоды одного сотрудника в один бар?
   - Вариант A: Каждая загрузка — отдельный бар
   - Вариант B: Смежные загрузки одного сотрудника объединяются

2. **Цветовая схема**: По чему определять цвет чипа?
   - Вариант A: По команде (все сотрудники одной команды — один цвет)
   - Вариант B: По сотруднику (каждый сотрудник — уникальный цвет)
   - Вариант C: По этапу (текущая логика в EmployeeRow)

3. **Высота строки**: Фиксированная или динамическая?
   - При многих перекрывающихся загрузках строка может стать очень высокой
