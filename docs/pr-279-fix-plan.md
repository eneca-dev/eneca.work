# PR #279 — План исправлений

**Ветка:** `Fixes/sections-and-departments`
**Дата аудита:** 2026-02-18
**Аудиторы:** performance-guardian, typescript-guardian, clean-code-guardian, forms-guardian, nextjs-guardian, ui-ux-advisor, modal-architect

---

## 🔴 Блокеры — обязательно до мержа

### 1. Убрать `as any` в `departments-timeline/EmployeeRow.tsx`

**Агент:** typescript-guardian
**Файл:** [modules/departments-timeline/components/timeline/EmployeeRow.tsx](modules/departments-timeline/components/timeline/EmployeeRow.tsx)

**Проблема 1 — line 173:**
```tsx
// ❌ Сейчас:
onLoadingClick(bar.period as any)

// ✅ Исправление:
if (bar.period.type === 'loading') {
  onLoadingClick(bar.period as Loading)
}
```

**Проблема 2 — line 604-607:**
```tsx
// ❌ Сейчас:
return loadingsToPeriods(employee.loadings as any)

// ✅ Исправление: явный маппинг типов или правильная типизация employee.loadings
```

**Что может сломаться:**
- Проблема 1: если `bar.period.type` никогда не равен `'loading'` в каком-то сценарии — клик по бару перестанет срабатывать без ошибки. Нужно убедиться, что тип `Loading` и его дискриминант `type` совпадают с реальными данными.
- Проблема 2: если тип `employee.loadings` реально несовместим с аргументом `loadingsToPeriods` (из-за nullable полей, опциональных свойств и т.д.) — TypeScript не скомпилирует код. Потребуется явный маппинг.

**Как проверить:**
- `tsc --noEmit` — должен пройти без ошибок в этом файле.
- В departments-timeline кликнуть по любому loading-бару → должен открываться модал редактирования загрузки.
- Строки с сотрудниками должны рендериться без ошибок в консоли.

---

### 2. Удалить дублирование `PeriodBackground` из `TimelineRow.tsx`

**Агент:** clean-code-guardian
**Файл:** [modules/resource-graph/components/timeline/TimelineRow.tsx](modules/resource-graph/components/timeline/TimelineRow.tsx)

`PeriodBackground` и `ResizablePeriodBackground` объявлены дважды:
- `modules/resource-graph/components/timeline/shared/PeriodBackground.tsx` ← **правильное место**
- `modules/resource-graph/components/timeline/TimelineRow.tsx` lines 932–1073 ← **удалить**

Компонент `shared/PeriodBackground.tsx` уже импортируется в `TimelineRow.tsx` на line 4 через `{ PeriodBackground }`. Локальная копия — мёртвый код.

**Что может сломаться:**
- Если локальная копия на lines 932–1073 _незаметно отличается_ от `shared/PeriodBackground.tsx` (пропсы, стили, логика), удаление приведёт к визуальным или поведенческим расхождениям.
- Если какой-то другой код в `TimelineRow.tsx` ниже line 932 ссылается на локальный вариант `ResizablePeriodBackground`, а не на импортированный — получим `ReferenceError` в рантайме.

**Как проверить:**
- Перед удалением: `diff` между shared-версией и локальной копией (props, classNames, внутренняя логика).
- `tsc --noEmit` после удаления.
- Открыть resource-graph, проверить визуальное отображение периодов (фон, границы, цвета) для каждого типа строки: проект / объект / секция / стадия декомпозиции.

---

### 3. Вынести дублированные утилиты в общий модуль

**Агент:** clean-code-guardian
**Файлы:**
- [modules/departments-timeline/components/timeline/EmployeeRow.tsx](modules/departments-timeline/components/timeline/EmployeeRow.tsx) — lines 47–103
- [modules/sections-page/components/rows/EmployeeRow.tsx](modules/sections-page/components/rows/EmployeeRow.tsx) — lines 61–127

Три функции полностью дублируются:

| Функция | departments-timeline | sections-page |
|---------|---------------------|---------------|
| `dayCellsToTimelineUnits` | line 47 | line 61 |
| `hexToRgba` | line 78 | line 83 |
| `calculateTimelineRange` | line 93 | line 116 |

**Решение:** создать `modules/resource-graph/utils/timeline-cell-utils.ts` (или добавить в существующий utils-файл) и экспортировать оттуда. Импортировать в обоих `EmployeeRow.tsx`.

> **Примечание:** версия `hexToRgba` в `sections-page/EmployeeRow.tsx` немного отличается (обрабатывает `rgba` prefix) — использовать более полную версию.

**Что может сломаться:**
- Если взять менее полную версию `hexToRgba` (без обработки `rgba` prefix) — в sections-page цвета барей могут сломаться при передаче `rgba(...)` строки вместо hex.
- Если `calculateTimelineRange` в двух версиях имеет разные крайние случаи (пустой массив, нулевые даты) — поведение одного из модулей изменится.
- Любая опечатка в новом пути импорта → runtime error (`Cannot find module`).

**Как проверить:**
- `tsc --noEmit` после рефакторинга.
- Открыть departments-timeline и sections-page, убедиться что бары сотрудников рендерятся с правильными цветами и корректной шириной/позицией.
- Передать цвет в формате `rgba(...)` хотя бы одному loading-у и проверить, что цвет не ломается.

---

## 🟡 Желательно — улучшения качества

### 4. Объединить 3 scroll-useEffect → 1 в обоих `EmployeeRow`

**Агент:** performance-guardian
**Файлы:**
- [modules/departments-timeline/components/timeline/EmployeeRow.tsx](modules/departments-timeline/components/timeline/EmployeeRow.tsx) — lines 187, 215, 246
- [modules/sections-page/components/rows/EmployeeRow.tsx](modules/sections-page/components/rows/EmployeeRow.tsx) — lines 198, 228, 256

Каждый `LoadingBar`/`LoadingBarWithResize` создаёт 3 отдельных scroll-листенера на один контейнер. При 20+ барах на экране = 60+ листенеров.

```tsx
// ✅ Один useEffect вместо трёх:
useEffect(() => {
  const container = textRef.current?.closest('.overflow-auto')
  if (!container) return

  const update = () => {
    const scrollLeft = (container as Element).scrollLeft
    const overlap = Math.max(0, scrollLeft - displayLeft)

    if (textRef.current) textRef.current.style.transform = `translateX(${overlap}px)`
    if (commentRef.current) commentRef.current.style.transform = `translateX(${overlap}px)`
    if (rateBadgeRef.current) {
      const clampedOffset = Math.min(overlap, Math.max(0, displayWidth - 48))
      rateBadgeRef.current.style.transform = `translateX(${clampedOffset}px)`
    }
  }

  update()
  container.addEventListener('scroll', update, { passive: true })
  return () => container.removeEventListener('scroll', update)
}, [displayLeft, displayWidth])
```

**Что может сломаться:**
- Если у трёх оригинальных `useEffect` были **разные dependency arrays**, объединение в один с `[displayLeft, displayWidth]` может привести к тому, что эффект не перезапустится при нужном изменении (или наоборот — запустится лишний раз).
- Если `closest('.overflow-auto')` не найдёт контейнер в одном из модулей (разная структура DOM) — scroll-позиционирование текста перестанет работать молча.

**Как проверить:**
- Прокрутить таймлайн горизонтально при наличии 10+ барей: текст загрузки, комментарий и rate-badge должны оставаться прижатыми к левому краю видимой области.
- Изменить масштаб/зум (если есть) — проверить, что бары и текст корректно перерисовываются.
- Проверить в обоих модулях: departments-timeline и sections-page.

---

### 5. Добавить `useCallback` для resize-handlers в `TimelineRow.tsx`

**Агент:** performance-guardian
**Файл:** [modules/resource-graph/components/timeline/TimelineRow.tsx](modules/resource-graph/components/timeline/TimelineRow.tsx) — lines 656, 666

```tsx
// ❌ Сейчас — новая функция на каждый рендер:
const handleLoadingResize = (loadingId: string, startDate: string, finishDate: string) => {
  updateLoadingDates.mutate(...)
}

// ✅ Исправление:
const handleLoadingResize = useCallback((loadingId: string, startDate: string, finishDate: string) => {
  updateLoadingDates.mutate({ loadingId, sectionId, startDate, finishDate })
}, [updateLoadingDates, sectionId])

const handleStageResize = useCallback((newStartDate: string, newFinishDate: string) => {
  updateStageDates.mutate({ stageId: stage.id, startDate: newStartDate, finishDate: newFinishDate })
}, [updateStageDates, stage.id])
```

**Что может сломаться:**
- Если `sectionId` или `stage.id` — нестабильные ссылки (объект/массив) и eslint-plugin-react-hooks не отловит это — `useCallback` закеширует стale-значение и при ресайзе в мутацию уйдут устаревшие данные.
- Если `updateLoadingDates` или `updateStageDates` — новые объекты на каждый рендер (не стабилизированы react-query), `useCallback` будет пересоздаваться каждый раз и не даст никакого выигрыша.

**Как проверить:**
- Выполнить ресайз loading-бара (потащить край): в Network tab должен уйти один запрос с правильными `loadingId`, `sectionId`, датами.
- Выполнить ресайз stage-бара: проверить корректные `stageId` и даты в запросе.
- Убедиться, что после ресайза данные обновились на экране (реактивность не сломана).

---

### 6. Исправить цвет кнопок в `RateInput.tsx` — teal → amber

**Агент:** modal-architect
**Файл:** [modules/modals/components/loading-modal-new/RateInput.tsx](modules/modals/components/loading-modal-new/RateInput.tsx) — line 97-99

```tsx
// ❌ Сейчас — teal (не соответствует Resource Graph design language):
'bg-teal-500 text-white hover:bg-teal-600 dark:bg-teal-600 dark:hover:bg-teal-700'

// ✅ Resource Graph standard:
'bg-amber-500 hover:bg-amber-400 text-slate-900'
```

**Что может сломаться:**
- Чисто визуальное изменение. В тёмной теме `bg-amber-500` без явного `dark:` варианта может выглядеть некорректно (оригинальный teal имел `dark:bg-teal-600`).
- Контраст `text-slate-900` на `bg-amber-500` — нужно проверить доступность (WCAG AA: минимум 4.5:1).

**Как проверить:**
- Открыть loading modal, переключить светлую/тёмную тему — кнопки `+`/`-` должны быть читаемы.
- Убедиться, что hover-состояние визуально ощутимо.

---

### 7. Удалить закомментированный код в `RateInput.tsx`

**Агент:** clean-code-guardian, forms-guardian
**Файл:** [modules/modals/components/loading-modal-new/RateInput.tsx](modules/modals/components/loading-modal-new/RateInput.tsx) — lines 126-130

```tsx
// ❌ Удалить этот блок:
{/* {!error && !isInvalidRange && (
  <p className="text-xs text-muted-foreground">
    Введите значение от 0.01 до 2.0
  </p>
)} */}
```

**Что может сломаться:**
- Ничего — закомментированный код не влияет на рантайм. Чисто косметическое изменение.

**Как проверить:**
- `tsc --noEmit`, убедиться что удаление не задело соседние строки (случайно стёрли jsx-тег).
- Визуально открыть RateInput — верстка не должна измениться.

---

### 8. Исправить отображение ставки в `sections-page/EmployeeRow.tsx`

**Агент:** ui-ux-advisor
**Файл:** [modules/sections-page/components/rows/EmployeeRow.tsx](modules/sections-page/components/rows/EmployeeRow.tsx) — lines 617-624

```tsx
// ❌ Сейчас — показывает максимальную ставку, а не сумму:
const maxRate = Math.max(...employee.loadings.map(l => l.rate))
return maxRate

// ✅ Показывать суммарную нагрузку:
const totalRate = employee.loadings.reduce((sum, l) => sum + l.rate, 0)
return Math.round(totalRate * 100) / 100
```

Пользователь должен видеть реальную суммарную нагрузку, а не максимальную ставку одной загрузки.

**Что может сломаться:**
- Если где-то в UI есть пороговая логика, рассчитанная под `maxRate` (например, «показать предупреждение если ставка > 1»), то с `totalRate` значение резко вырастет и предупреждение будет показываться неожиданно.
- Если у сотрудника нет загрузок (`loadings = []`): `Math.max(...[])` возвращает `-Infinity`, а `reduce(..., 0)` вернёт `0` — это исправление дополнительно фиксирует этот edge case.

**Как проверить:**
- Открыть sections-page, найти сотрудника с несколькими загрузками (например, 0.5 + 0.5 + 0.3) — отображаемая ставка должна быть `1.3`, а не `0.5`.
- Найти сотрудника без загрузок — должен отображаться `0` без ошибок.
- Проверить, что предупреждение о перегрузке (если есть) срабатывает при `totalRate > 1`, а не при одной высокой ставке.

---

### 9. Переименовать boolean `hoveredAvatar` в `isHoveredAvatar`

**Агент:** clean-code-guardian
**Файлы:**
- [modules/departments-timeline/components/timeline/EmployeeRow.tsx](modules/departments-timeline/components/timeline/EmployeeRow.tsx) — line 506
- [modules/sections-page/components/rows/EmployeeRow.tsx](modules/sections-page/components/rows/EmployeeRow.tsx) — line 527

```tsx
// ❌ Сейчас:
const [hoveredAvatar, setHoveredAvatar] = useState(false)

// ✅ По конвенции is* для булевых:
const [isHoveredAvatar, setIsHoveredAvatar] = useState(false)
```

**Что может сломаться:**
- Если переменная `hoveredAvatar` использована в JSX или в других местах компонента и хотя бы одно вхождение пропустить при переименовании — `ReferenceError` в рантайме.
- Если `hoveredAvatar` передаётся как prop дочернему компоненту с явным именем — нужно переименовать и там.

**Как проверить:**
- `tsc --noEmit` — не должно быть `Cannot find name 'hoveredAvatar'`.
- Навести мышь на аватар сотрудника в обоих модулях: должен появляться тултип/попап как раньше.

---

### 10. Вынести `formatDate` из `onChange` callback в `LoadingForm.tsx`

**Агент:** forms-guardian
**Файл:** [modules/modals/components/loading-modal-new/LoadingForm.tsx](modules/modals/components/loading-modal-new/LoadingForm.tsx) — lines 167-173

```tsx
// ❌ Сейчас — объявляется внутри JSX callback при каждом рендере:
onChange={(range) => {
  const formatDate = (date: Date | null) => { ... }
  ...
}}

// ✅ Module-level или вне компонента:
function formatLocalDate(date: Date | null): string {
  if (!date) return ''
  const y = date.getFullYear()
  const m = String(date.getMonth() + 1).padStart(2, '0')
  const d = String(date.getDate()).padStart(2, '0')
  return `${y}-${m}-${d}`
}
```

**Что может сломаться:**
- Если оригинальная inline-функция использовала переменную из closure (timezone offset, локаль) — вынос на module-level изменит поведение.
- Смена имени `formatDate` → `formatLocalDate`: убедиться, что старое имя нигде не используется снаружи.

**Как проверить:**
- Открыть loading modal, выбрать диапазон дат — в Network запросе на сохранение даты должны быть в формате `YYYY-MM-DD` без timezone-смещения.
- Проверить граничные даты: 31 декабря / 1 января — корректный формат без сдвига на предыдущий/следующий день.

---

### 11. Заменить IIFE в render `LoadingForm.tsx` на переменные

**Агент:** forms-guardian
**Файл:** [modules/modals/components/loading-modal-new/LoadingForm.tsx](modules/modals/components/loading-modal-new/LoadingForm.tsx) — lines 185-200

```tsx
// ❌ Сейчас — IIFE в JSX:
{(() => {
  const start = ...
  const end = ...
  return businessDays > 0 ? <div>...</div> : null
})()}

// ✅ Переменные до return:
const start = formData.startDate ? new Date(formData.startDate) : undefined
const end = formData.endDate ? new Date(formData.endDate) : undefined
const businessDays = start && end && !isNaN(start.getTime()) && !isNaN(end.getTime())
  ? differenceInBusinessDays(end, start) + 1
  : 0
const totalHours = businessDays > 0 ? Math.round(businessDays * 8 * formData.rate) : 0

// В JSX:
{businessDays > 0 && (
  <div className="text-xs text-muted-foreground space-y-1">
    <p>Количество рабочих дней: {businessDays}</p>
    <p>Количество рабочих часов с учётом ставки: {totalHours} ч</p>
  </div>
)}
```

**Что может сломаться:**
- Если новые переменные `start`/`end`/`businessDays` конфликтуют по имени с уже существующими в том же компоненте — TypeScript-ошибка или неверное затенение.
- Если `differenceInBusinessDays(end, start) + 1` дает отрицательный результат при `end < start` — `totalHours` будет отрицательным. IIFE мог скрывать этот случай.

**Как проверить:**
- Выбрать диапазон 5 рабочих дней — должно показать `5 дней`, `40 ч` (при ставке 1.0).
- Выбрать диапазон со ставкой 0.5 — `5 дней`, `20 ч`.
- Выбрать дату конца раньше начала (если UI позволяет) — не должно показывать отрицательные значения.
- Выбрать диапазон только с выходными — `0 дней`, блок не отображается.

---

## 🔵 Технический долг — при наличии времени

### 12. Добавить именованные константы в `AggregatedBarsOverlay.tsx`

**Агент:** clean-code-guardian
**Файл:** [modules/sections-page/components/AggregatedBarsOverlay.tsx](modules/sections-page/components/AggregatedBarsOverlay.tsx) — lines 41-78

```tsx
// ❌ Magic numbers:
if (percentage > 100) ...
if (percentage >= 95) ...
if (percentage >= 70) ...

// ✅ Named constants:
const OVERLOAD_THRESHOLD = 100
const IDEAL_LOAD_MIN = 95
const HIGH_LOAD_MIN = 70
const MEDIUM_LOAD_MIN = 40
```

**Что может сломаться:**
- Ничего — чисто рефакторинг без изменения значений. Риск только в опечатке (например, написать `95` вместо `96` при переносе).
- Если эти же магические числа продублированы в других файлах и не синхронизированы с константами — поведение в других местах останется прежним.

**Как проверить:**
- `tsc --noEmit`.
- В sections-page проверить цвета оверлея при разных процентах загрузки: >100% → красный/критический, 95-100% → оптимальный, 70-95% → высокий, 40-70% → средний, <40% → низкий.

---

### 13. Обернуть `LoadingBar`/`LoadingBarWithResize` в `React.memo`

**Агент:** performance-guardian
**Файлы:**
- `departments-timeline/components/timeline/EmployeeRow.tsx` — `LoadingBarWithResize` (line 118)
- `sections-page/components/rows/EmployeeRow.tsx` — `LoadingBar` (line 141)

```tsx
const LoadingBar = memo(function LoadingBar({ ... }: LoadingBarProps) {
  // ...
})
```

**Что может сломаться:**
- `React.memo` делает поверхностное сравнение props. Если проп — нестабильная ссылка (inline объект/массив/функция, создаваемая в родителе на каждый рендер) — memo не даст выигрыша и _может_ маскировать пропущенные ре-рендеры.
- Если компонент имеет внутренние side effects, зависящие от рендера при каждом изменении родителя — memo может сломать логику.

**Как проверить:**
- Добавить/изменить загрузку → список должен обновиться (новый/изменённый бар виден).
- Drag-resize бара → размер должен обновляться в реальном времени (memo не заблокировал обновления).
- В React DevTools Profiler: убедиться, что неизменённые бары не перерисовываются при скролле или обновлении других данных.

---

### 14. Разбить `TimelineRow.tsx` на отдельные файлы

**Агент:** clean-code-guardian
**Файл:** [modules/resource-graph/components/timeline/TimelineRow.tsx](modules/resource-graph/components/timeline/TimelineRow.tsx) — 1637 строк

Предлагаемая структура:
```
modules/resource-graph/components/timeline/
├── rows/
│   ├── ProjectRow.tsx
│   ├── ObjectRow.tsx
│   ├── SectionRow.tsx
│   ├── DecompositionStageRow.tsx
│   └── DecompositionItemRow.tsx
├── shared/
│   ├── PeriodBackground.tsx  ← уже существует ✅
│   ├── ProgressCircle.tsx    ← уже существует ✅
│   ├── TimelineGrid.tsx      ← уже существует ✅
│   └── BaseRow.tsx           ← вынести из TimelineRow.tsx
└── TimelineRow.tsx           ← только export точка входа
```

**Что может сломаться:**
- Это самое рискованное изменение в техдолге. Ошибка в одном re-export → весь resource-graph перестаёт работать.
- Если между row-компонентами есть взаимные зависимости (общие локальные хелперы внутри файла) — при разбивке нужно правильно вынести их в shared/, иначе circular imports или дублирование.
- Любой пропущенный экспорт в entry-point `TimelineRow.tsx` → `undefined is not a function` в рантайме.

**Как проверить:**
- `tsc --noEmit` — ноль ошибок.
- Открыть resource-graph и прокликать все типы строк: project / object / section / decomposition stage / decomposition item — каждая должна рендериться и разворачиваться.
- Ресайз и клики по всем типам строк.
- Убедиться что bundle не вырос аномально (circular dependency может вызвать дублирование кода).

---

### 15. Привести labels в `LoadingForm.tsx` к дизайн-стандарту

**Агент:** modal-architect
**Файл:** [modules/modals/components/loading-modal-new/LoadingForm.tsx](modules/modals/components/loading-modal-new/LoadingForm.tsx)

```tsx
// ❌ Сейчас:
<label className="block text-sm font-medium mb-2">Сотрудник</label>

// ✅ Resource Graph standard:
<label className="block text-[10px] font-medium text-slate-400 uppercase tracking-wide mb-2">
  СОТРУДНИК
</label>
```

**Что может сломаться:**
- `text-[10px]` меньше текущего `text-sm` (14px) — лейблы визуально уменьшатся, расстояние до поля может сжаться или нарушить ритм макета.
- В тёмной теме `text-slate-400` без `dark:` варианта может быть слишком тёмным и нечитаемым.
- `uppercase` изменит текст без изменения исходного контента — если где-то лейбл используется как aria-label или в тестах по тексту, это сломает.

**Как проверить:**
- Открыть loading modal в светлой и тёмной теме, убедиться что все лейблы читаемы и не сливаются с фоном.
- Пройтись по всем полям формы: сотрудник, даты, ставка — лейблы должны быть видны над каждым полем.

---

## Чеклист

### 🔴 Блокеры
- [x] Убрать `as any` × 2 в `departments-timeline/EmployeeRow.tsx`
- [x] Удалить дублированный `PeriodBackground` из `TimelineRow.tsx` (lines 932–1073)
- [x] Вынести `dayCellsToTimelineUnits`, `hexToRgba`, `calculateTimelineRange` в общий модуль

### 🟡 Желательно
- [ ] Объединить 3 scroll-useEffect → 1 в обоих `EmployeeRow`
- [ ] Добавить `useCallback` для resize-handlers в `TimelineRow.tsx`
- [ ] Исправить цвет кнопок в `RateInput.tsx` — teal → amber
- [ ] Удалить закомментированный код в `RateInput.tsx`
- [ ] Исправить `employmentRate` max → sum в `sections-page/EmployeeRow.tsx`
- [ ] Переименовать `hoveredAvatar` → `isHoveredAvatar`
- [ ] Вынести `formatDate` из `onChange` callback в `LoadingForm.tsx`
- [ ] Заменить IIFE в render `LoadingForm.tsx` на переменные

### 🔵 Технический долг
- [ ] Добавить константы для thresholds в `AggregatedBarsOverlay.tsx`
- [ ] `React.memo` для `LoadingBar` / `LoadingBarWithResize`
- [ ] Разбить `TimelineRow.tsx` (1637 строк) на отдельные файлы
- [ ] Привести labels в `LoadingForm.tsx` к дизайн-стандарту
