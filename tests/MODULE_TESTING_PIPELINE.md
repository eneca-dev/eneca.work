# Пайплайн создания E2E тестов для нового модуля

> Методология создания комплексных E2E тестов для изолированного модуля приложения.

## 📋 Предварительные требования

Перед созданием тестов для модуля убедитесь, что **общая инфраструктура** проекта уже настроена (настраивается один раз для всего проекта):

- ✅ `tests/auth.setup.ts` — скрипт глобальной авторизации.
- ✅ `tests/.auth/admin.json` — файл сохранённой сессии (генерируется скриптом).
- ✅ `tests/fixtures/auth.fixture.ts` — кастомные фикстуры для тестов.
- ✅ `playwright.config.ts` — базовая конфигурация Playwright.

*Если этого нет, обратитесь к `tests/README.md`.*

---

## 🎯 Этапы работы

### Фаза 1: Изучение модуля (Research)

**Цель:** Понять архитектуру и функциональность до написания кода.

1.  **Изучить UI компоненты:**
    *   Страницы/экраны.
    *   Формы и списки.
    *   Модальные окна и интерактивные элементы.
2.  **Определить стратегии селекторов:**
    *   Искать `data-*` атрибуты.
    *   Использовать Role атрибуты (`[role="dialog"]`).
    *   Текстовые метки (для кнопок).
3.  **Составить список сценариев:**
    *   Базовая видимость.
    *   Взаимодействие (CRUD операции).
    *   Валидация полей.
    *   Сохранение состояния (фильтры, сортировка).

---

### Фаза 2: Структура файлов

Создайте следующую структуру папок для модуля `tests/{module_name}/`:

```
tests/{module_name}/
├── README.md                           # Стратегия тестирования модуля
├── constants/
│   └── selectors.ts                    # Централизованные селекторы
├── helpers/
│   └── {module}-helper.ts              # Helper класс (Page Object)
├── {feature1}/
│   └── basic.spec.ts                   # Тесты функциональности 1
└── {feature2}/
    └── search.spec.ts                  # Тесты функциональности 2
```

---

### Фаза 3: Централизация селекторов

**Файл:** `tests/{module_name}/constants/selectors.ts`
Вся работа с DOM-элементами должна идти через константы. Не хардкодьте строки в тестах.

```typescript
export const SELECTORS = {
  // Контейнеры
  containers: {
    main: '.main-container',
  },
  // Кнопки
  buttons: {
    save: 'button:has-text("Сохранить")',
    create: '[data-testid="create-btn"]',
  },
  // Формы
  forms: {
    nameInput: 'input[name="name"]',
  },
  // Списки
  lists: {
    item: '.list-item',
  },
}

export const TIMEOUTS = {
  SHORT: 500,
  MEDIUM: 1000,
  LONG: 3000,
}
```

---

### Фаза 4: Создание Helper класса (Page Object)

**Файл:** `tests/{module_name}/helpers/{module}-helper.ts`
Класс должен инкапсулировать логику взаимодействия, скрывая детали реализации Playwright.

```typescript
import { Page, Locator } from '@playwright/test'
import { SELECTORS, TIMEOUTS } from '../constants/selectors'

export class ModuleHelper {
  constructor(private page: Page) {}

  // Навигация
  async goto(): Promise<void> {
    await this.page.goto('/module-path')
    await this.page.waitForLoadState('networkidle')
  }

  // Действия
  async createItem(name: string): Promise<void> {
    await this.page.locator(SELECTORS.buttons.create).click()
    await this.page.locator(SELECTORS.forms.nameInput).fill(name)
    await this.page.locator(SELECTORS.buttons.save).click()
    await this.page.waitForTimeout(TIMEOUTS.MEDIUM)
  }

  // Утилиты
  async getItemsCount(): Promise<number> {
    return await this.page.locator(SELECTORS.lists.item).count()
  }
}
```

---

### Фаза 5: Написание тестов

**Файл:** `tests/{module_name}/{feature}/{test}.spec.ts`

Используйте паттерн **"Load Once, Reset Often"**:
1.  Загружайте страницу один раз в `beforeAll` используя сохраненный стейт.
2.  Сбрасывайте состояние (фильтры, формы) в `afterEach`.
3.  Избегайте `reload()` страницы без крайней необходимости.

```typescript
import { test, expect, Page } from '@playwright/test' // или import { test } from '../../fixtures/auth.fixture'
import { ModuleHelper } from '../helpers/module-helper'

test.describe('Module Feature Tests', () => {
  let helper: ModuleHelper
  let sharedPage: Page

  // 1. Инициализация (Один раз на файл)
  test.beforeAll(async ({ browser }) => {
    // Используем путь к файлу из Предварительных требований
    const context = await browser.newContext({ storageState: 'tests/.auth/admin.json' })
    sharedPage = await context.newPage()
    helper = new ModuleHelper(sharedPage)
    await helper.goto()
  })

  // 2. Очистка (После каждого теста)
  test.afterEach(async () => {
    // Логика сброса фильтров или закрытия модалок, чтобы следующий тест начал с чистого листа
  })

  test.afterAll(async () => {
    await sharedPage.close()
  })

  // 3. Тесты
  test('element should be visible', async () => {
    await helper.createItem('Test Item')
    const count = await helper.getItemsCount()
    expect(count).toBeGreaterThan(0)
  })
})
```

---

## 🎓 Лучшие практики (Do's & Don'ts)

### ✅ DO
*   **Используйте `beforeAll`** для тяжелой инициализации страницы.
*   **Выносите селекторы** в отдельный файл констант.
*   **Создавайте методы в Helper**, даже для простых действий (кликов).
*   **Группируйте тесты** по функциональности (один файл = одна фича).
*   **Добавляйте явные ожидания** (`waitForTimeout` или `waitForSelector`), где есть анимации.

### ❌ DON'T
*   **НЕ используйте `beforeEach`** для полной перезагрузки страницы (это замедляет тесты).
*   **НЕ пишите селекторы** прямо в коде теста (`page.locator('.div > span')`).
*   **НЕ дублируйте логику** — если код повторяется, вынесите его в helper.
*   **НЕ делайте `page.reload()`** в каждом тесте (только для тестов на сохранение состояния при перезагрузке).

---

## 🔧 Чек-лист готовности модуля

1.  [ ] Создан файл `constants/selectors.ts` (нет хардкода строк).
2.  [ ] Реализован `helpers/{module}-helper.ts` (нет логики Playwright в файлах `.spec.ts`).
3.  [ ] Тесты разбиты по папкам/файлам согласно функциональности.
4.  [ ] Используется оптимизация `beforeAll` и `storageState`.
5.  [ ] Тесты запускаются и проходят стабильно (`--repeat-each=3`).