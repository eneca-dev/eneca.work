# Тестирование фильтров модуля Resource Graph

E2E тесты для проверки всех фильтров под ролью **admin**.

---

## Цель

Проверить что все фильтры работают корректно:
- Одиночные фильтры применяются без ошибок
- Каскадное очищение зависимых фильтров
- Комбинации фильтров
- Disabled состояния
- Данные фильтруются правильно
- Сохранение в localStorage

---

## Система фильтрации (7 фильтров)

### Проектные фильтры (2 фильтра):
1. **managerId** (Руководитель) → очищает projectId
2. **projectId** (Проект) → конечный фильтр

> **Примечание:** Стадия, Объект, Раздел отображаются в дереве структуры слева, но не являются фильтрами.

### Организационная иерархия (4 фильтра, каскадные):
3. **subdivisionId** (Подразделение) → очищает departmentId, teamId, employeeId
4. **departmentId** (Отдел) → очищает teamId, employeeId
5. **teamId** (Команда) → очищает employeeId
6. **employeeId** (Сотрудник) → конечный фильтр

### Дополнительные (1 фильтр):
7. **tagIds** (Теги проектов) → мульти-выбор, независимый

---

## Тесты для проведения

### 1. Smoke Test (`smoke.spec.ts`) - 1 тест

**Проверки:**
- [ ] Страница загружается без ошибок
- [ ] Фильтры отображаются
- [ ] Нет JavaScript ошибок в консоли

---

### 2. Single Filters (`filters/single-filters.spec.ts`) - 7 тестов

Тестирование каждого фильтра отдельно:

**2.1. Фильтр по руководителю (managerId)**
- [ ] Фильтр применяется
- [ ] Сохраняется в localStorage
- [ ] Данные фильтруются корректно

**2.2. Фильтр по проекту (projectId)**
- [ ] Фильтр применяется независимо от managerId
- [ ] Сохраняется в localStorage
- [ ] Данные фильтруются корректно

**2.3. Фильтр по подразделению (subdivisionId)**
- [ ] Фильтр применяется
- [ ] Сохраняется в localStorage
- [ ] Данные фильтруются корректно

**2.4. Фильтр по отделу (departmentId)**
- [ ] Фильтр применяется
- [ ] Сохраняется в localStorage
- [ ] Данные фильтруются корректно

**2.5. Фильтр по команде (teamId)**
- [ ] Требует выбранного departmentId (disabled без него)
- [ ] Фильтр применяется
- [ ] Данные фильтруются корректно

**2.6. Фильтр по сотруднику (employeeId)**
- [ ] Требует выбранного teamId (disabled без него)
- [ ] Фильтр применяется
- [ ] Данные фильтруются корректно

**2.7. Мульти-выбор тегов (tagIds)**
- [ ] Можно выбрать несколько тегов
- [ ] Данные фильтруются по всем выбранным тегам
- [ ] Сохраняется в localStorage

---

### 3. Cascading Filters (`filters/cascading-filters.spec.ts`) - 6 тестов

#### 3.1. Каскадное очищение (4 теста)

**Проектные фильтры:**
- [ ] Смена managerId очищает projectId

**Организационная иерархия:**
- [ ] Смена subdivisionId очищает departmentId, teamId, employeeId
- [ ] Смена departmentId очищает teamId, employeeId
- [ ] Смена teamId очищает employeeId

#### 3.2. Disabled состояния (2 теста)

**Организационная иерархия:**
- [ ] teamId disabled пока не выбран departmentId
- [ ] employeeId disabled пока не выбран teamId

---

### 4. Combined Filters (`filters/combined-filters.spec.ts`) - 9 тестов

**Комбинации иерархий:**
- [ ] Проектные + организационные фильтры работают вместе
- [ ] Теги + проектные фильтры
- [ ] Теги + организационные фильтры

**Максимальные комбинации:**
- [ ] Все фильтры одновременно (managerId + projectId + орг + теги)

**Переключения:**
- [ ] Переключение между разными руководителями
- [ ] Переключение между разными проектами
- [ ] Переключение между разными сотрудниками

**Динамические изменения:**
- [ ] Добавление/удаление тегов при активных фильтрах

**Граничные случаи:**
- [ ] Пустой результат фильтрации (нет совпадений)

---

### 5. Reset Filters (`filters/reset-filters.spec.ts`) - 5 тестов

- [ ] Кнопка "Сбросить все" очищает все фильтры
- [ ] Кнопка "Сбросить проектные" очищает managerId и projectId
- [ ] Кнопка "Сбросить организационные" очищает орг. иерархию
- [ ] Последовательные сбросы работают корректно
- [ ] Сброс при пустых фильтрах (no-op)

---

### 6. Tag Filters (`filters/tag-filters.spec.ts`) - 6 тестов

- [ ] Выбор одного тега
- [ ] Мульти-выбор тегов (2-5 тегов)
- [ ] Снятие выбора тега (toggle off)
- [ ] Выбор всех тегов
- [ ] Последовательное добавление/удаление тегов
- [ ] Теги при активных иерархических фильтрах

---

### 7. LocalStorage (`filters/localStorage.spec.ts`) - 3 теста

- [ ] Фильтры сохраняются при перезагрузке страницы
- [ ] Восстановление состояния после закрытия/открытия вкладки
- [ ] Очистка localStorage при сбросе фильтров

---

## Итоговая статистика

| Категория | Файл | Тестов | Приоритет |
|-----------|------|--------|-----------|
| Smoke | `smoke.spec.ts` | 1 | 🔴 HIGH |
| Single Filters | `filters/single-filters.spec.ts` | 7 | 🔴 HIGH |
| Cascading Filters | `filters/cascading-filters.spec.ts` | 6 | 🔴 HIGH |
| Combined Filters | `filters/combined-filters.spec.ts` | 9 | 🟡 MEDIUM |
| Reset Filters | `filters/reset-filters.spec.ts` | 5 | 🔴 HIGH |
| Tag Filters | `filters/tag-filters.spec.ts` | 6 | 🟡 MEDIUM |
| LocalStorage | `filters/localStorage.spec.ts` | 3 | 🟢 LOW |
| **ИТОГО** | | **37 тестов** | |

---

## Проверки для каждого теста

Для всех тестов проверять:
1. ✅ Фильтр сохраняется в localStorage
2. ✅ Данные фильтруются корректно
3. ✅ Нет ошибок в консоли
4. ✅ UI обновляется правильно
