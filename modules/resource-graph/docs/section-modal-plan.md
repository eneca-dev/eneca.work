# SectionModal — План реализации

> **Спринт 4.1** | Модалка раздела для resource-graph
> **Дата:** 2024-12-16

---

## Требования

### UX/UI
- **Slide-in панель** справа (не центрированная модалка)
- **Моментальная загрузка** — данные уже есть в кеше (Section из useResourceGraphData)
- **Компактный дизайн** — минимум отступов, плотная информация
- **Dark theme** — glass-morphic стиль

### Структура

```
┌─────────────────────────────────────────┐
│ [X]                          ← Close    │
│                                         │
│ ┌─────┐ Название раздела               │
│ │ AVA │ Иванов И.И. (ответственный)    │
│ └─────┘ Статус: В работе               │
├─────────────────────────────────────────┤
│ [Общая] [Этапы]              ← Tabs    │
├─────────────────────────────────────────┤
│                                         │
│ 📅 Сроки: 01.01 — 31.03                │
│                                         │
│ 📝 Описание                             │
│ Lorem ipsum dolor sit amet...           │
│                                    [✏️] │
│                                         │
│ ─────────────────────────────────────── │
│                                         │
│ 📊 Показатели                           │
│ ┌─────────┬─────────┬─────────┐        │
│ │ План    │ Факт    │ Бюджет  │        │
│ │ 45%     │ 38%     │ 520K BYN│        │
│ └─────────┴─────────┴─────────┘        │
│                                         │
│ ─────────────────────────────────────── │
│                                         │
│ 🕐 Последняя активность                 │
│ • Иванов обновил готовность (2ч назад) │
│ • Петров добавил отчёт (вчера)         │
│                                         │
│ ─────────────────────────────────────── │
│                                         │
│ 💬 Комментарии (3)                      │
│ ┌─────────────────────────────────────┐│
│ │ CommentsPanel component             ││
│ └─────────────────────────────────────┘│
│                                         │
└─────────────────────────────────────────┘
```

---

## Технический план

### Этап 1: Базовая структура

**Файлы:**
- `modules/resource-graph/components/modals/SectionModal.tsx` — основной компонент
- `modules/resource-graph/components/modals/index.ts` — экспорт

**Компонент:**
```tsx
interface SectionModalProps {
  isOpen: boolean
  onClose: () => void
  section: Section  // Данные уже есть, не грузим
  sectionId: string // Для комментариев
}
```

**Особенности:**
- Slide-in справа (width: 480px)
- Backdrop с onClick={onClose}
- Анимация появления (translate-x)
- ESC для закрытия

### Этап 2: Header секция

**Содержимое:**
- Avatar ответственного (24x24)
- Название раздела (editable)
- Имя ответственного (dropdown для смены)
- Status chip (dropdown для смены)

**Редактирование:**
- Inline edit для названия
- Dropdown с поиском для ответственного
- Dropdown для статуса

### Этап 3: Tabs

**Вкладки:**
1. **Общая** — основная информация (реализуем)
2. **Этапы** — список decompositionStages (placeholder на будущее)

**Компонент:** Простые inline buttons с активным состоянием

### Этап 4: Вкладка "Общая"

**Блоки:**

#### 4.1 Сроки
- DateRangePicker (startDate — endDate)
- Inline edit

#### 4.2 Описание
- Текстовое поле (section_description)
- Редактирование через textarea
- Кнопка "Редактировать"

#### 4.3 Показатели (read-only)
- План: интерполированное значение на сегодня из readinessCheckpoints
- Факт: последнее значение из actualReadiness
- Бюджет: сумма потраченного из budgetSpending

#### 4.4 Последняя активность
- Последние 3 события (отчёты, изменения статуса)
- Источник: work_logs + section audit?
- Компактный формат: "Кто + Что + Когда"

#### 4.5 Комментарии
- Готовый компонент `CommentsPanel` из `/modules/comments/`
- Props: `sectionId={sectionId}`

### Этап 5: Server Actions

**Существующие (переиспользуем):**
- Комментарии уже работают через `/modules/comments/api/`

**Новые:**
```typescript
// В modules/resource-graph/actions/index.ts
export async function updateSectionDetails(
  sectionId: string,
  data: {
    name?: string
    description?: string
    responsibleId?: string
    statusId?: string
    startDate?: string
    endDate?: string
  }
): Promise<ActionResult<Section>>
```

### Этап 6: Интеграция

**В TimelineRow.tsx:**
```tsx
// SectionRow
const [isSectionModalOpen, setIsSectionModalOpen] = useState(false)

// При клике на название раздела
onClick={() => setIsSectionModalOpen(true)}

// Рендер модалки
<SectionModal
  isOpen={isSectionModalOpen}
  onClose={() => setIsSectionModalOpen(false)}
  section={section}
  sectionId={section.id}
/>
```

---

## Зависимости

### Уже готово:
- ✅ Тип `Section` в `types/index.ts`
- ✅ Система комментариев (`/modules/comments/`)
- ✅ Таблица `section_comments`
- ✅ Данные в кеше (useResourceGraphData)
- ✅ Dropdown с поиском (паттерн из SectionPanel)
- ✅ Glass-morphic стили

### Нужно проверить:
- [ ] Есть ли view для "последней активности"?
- [ ] Какие статусы доступны? (section_statuses)

---

## Не делаем сейчас

1. **Вкладка "Этапы"** — placeholder, реализуем позже
2. **Создание этапов** — отдельная задача
3. **Редактирование checkpoints** — отдельная задача
4. **Удаление раздела** — не требуется

---

## Оценка сложности

| Этап | Описание | Сложность |
|------|----------|-----------|
| 1 | Базовая структура slide-in | Низкая |
| 2 | Header с редактированием | Средняя |
| 3 | Tabs | Низкая |
| 4.1-4.3 | Сроки, описание, показатели | Низкая |
| 4.4 | Последняя активность | Средняя (нужен источник данных) |
| 4.5 | Комментарии | Низкая (готовый компонент) |
| 5 | Server Action | Низкая |
| 6 | Интеграция | Низкая |

**Общая оценка:** Средняя сложность, большая часть переиспользуется

---

## Альтернативы (для обсуждения)

### "Последняя активность"

**Вариант A:** Отдельный запрос к work_logs + section audit
- Плюс: Точные данные
- Минус: Дополнительный запрос

**Вариант B:** Показывать только из уже загруженных workLogs
- Плюс: Нет дополнительных запросов
- Минус: Только если раздел был развёрнут

**Вариант C:** Убрать блок, оставить только комментарии
- Плюс: Простота
- Минус: Меньше информации

**Рекомендация:** Вариант C для MVP, добавим активность позже
