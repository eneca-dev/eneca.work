# Resource Graph Module - План завершения

> Дата создания: 2024-12-15
> Ветка: `module/resource-graph`

---

## Обзор задач

Модуль находится в стадии активной разработки. Необходимо завершить функционал по 4 основным блокам:
1. **Раздел (Section)** — модалки, бюджеты, статусы
2. **Этап декомпозиции (DecompositionStage)** — вехи, статусы, валидация
3. **Задача (DecompositionItem)** — готовность, графики
4. **Общее** — отчёты, бюджеты, оптимизация, тестирование

---

## 🎯 Ближайшие действия

> **Обновлено:** 2024-12-15

### Спринт 1 — ✅ ВЫПОЛНЕНО

| # | Задача | Приоритет | Статус |
|---|--------|-----------|--------|
| 1 | Показать бюджет в тултипе отчёта | 🔴 Высокий | ✅ |
| 2 | Уменьшить и сделать прозрачной галочку готовности задачи | 🟡 Средний | ✅ |
| 3 | Убрать loading при открытии WorkLogCreateModal | 🟡 Средний | ✅ |
| 4 | Кешировать список сотрудников (cache module) | 🟡 Средний | ✅ |

### Спринт 2 — ✅ ВЫПОЛНЕНО

| # | Задача | Приоритет | Статус |
|---|--------|-----------|--------|
| 1 | Убрать статусы разделов (1.3) | 🟡 Средний | ✅ |
| 2 | Готовые этапы — серый цвет (2.2) | 🟡 Средний | ✅ |
| 3 | Статусы этапов (2.3) | 🟡 Средний | ✅ (уже было) |
| 4 | Показатели при наведении на раздел (1.4) | 🟡 Средний | ✅ |

### Спринт 3 (Блок A) — ✅ ВЫПОЛНЕНО

| # | Задача | Приоритет | Статус |
|---|--------|-----------|--------|
| A1 | Server Action updateItemProgress | 🔴 Высокий | ✅ |
| A2 | ProgressUpdateDialog — диалог обновления готовности | 🔴 Высокий | ✅ |
| A3 | Интеграция в WorkLogCreateModal | 🔴 Высокий | ✅ |
| A4 | Кликабельное название раздела (подготовка к модалке) | 🟡 Средний | ✅ |

### Реализовано:

#### 1. Показать бюджет в тултипе отчёта ✅
**Файлы:**
- `types/index.ts` — расширен тип WorkLog, добавлено поле `budget`
- `actions/index.ts` — обновлён getWorkLogsForSection, подгружает budgets + budget_types
- `components/timeline/WorkLogMarkers.tsx` — добавлено отображение бюджета в тултипе

#### 2. Уменьшить и сделать прозрачной галочку готовности ✅
**Файл:** `components/timeline/TimelineRow.tsx`
- ProgressCircle при 100% теперь меньше (70% от size)
- Добавлена прозрачность (opacity-50)
- Добавлен hover-эффект (opacity-80)
- Цвет фона и галочки — полупрозрачный зелёный

#### 3. Убрать loading при открытии модалки отчётов ✅
**Файл:** `modules/modals/components/worklog/WorkLogCreateModal.tsx`
- Используется `useUsers()` из cache module
- Используется `useCurrentUser()` для текущего пользователя
- Пользователи кешируются на 10 минут (staleTime: static)

#### 4. Кешировать список сотрудников ✅
**Файлы:**
- `modules/cache/actions/users.ts` — Server Action `getUsers()` и `getCurrentUser()`
- `modules/cache/hooks/use-users.ts` — хуки `useUsers()` и `useCurrentUser()`
- `modules/cache/index.ts` — экспорт всех новых сущностей

#### 5. Убрать статусы разделов (1.3) ✅
**Файл:** `components/timeline/TimelineRow.tsx`
- Удалён status chip из второй строки SectionRow
- Статус остаётся только в модалке раздела (когда будет создана)

#### 6. Готовые этапы — серый цвет (2.2) ✅
**Файл:** `components/timeline/TimelineRow.tsx`
- Добавлена проверка `isCompleted` (readiness >= 100 или статус "завершён")
- При завершённости применяется `opacity-50 grayscale-[30%]`
- Эффект распространяется на всю строку этапа

#### 7. Статусы этапов (2.3) ✅
**Файл:** `components/timeline/TimelineRow.tsx`
- Уже было реализовано ранее в `DecompositionStageRow`
- Status chip показывается во второй строке (после Progress + Hours)

#### 8. Показатели при наведении на раздел (1.4) ✅
**Файлы:**
- `components/timeline/SectionTooltipOverlay.tsx` — новый компонент
- `components/timeline/TimelineRow.tsx` — интеграция в SectionRow
- `components/timeline/index.ts` — экспорт

**Реализация:**
- Прозрачный слой поверх графиков раздела
- При наведении на любой день показывает тултип со всеми метриками:
  - План готовности (зелёный, пунктирная линия)
  - Факт готовности (синий, заливка)
  - Бюджет освоен (оранжевый/красный, % и сумма)
- Интерполяция значений между точками данных
- Glass-morphic стиль тултипа для dark theme
- Форматирование даты на русском языке

#### 9. Server Action updateItemProgress (A1) ✅
**Файл:** `actions/index.ts`
- Добавлена мутация `updateItemProgress(itemId, progress)`
- Валидация: progress 0-100, округление до целого
- UPDATE `decomposition_items.decomposition_item_progress`

#### 10. ProgressUpdateDialog (A2) ✅
**Файлы:**
- `modules/modals/components/progress/ProgressUpdateDialog.tsx` — новый компонент
- `modules/modals/components/progress/index.ts` — экспорт
- `modules/modals/index.ts` — публичный экспорт

**Функционал:**
- Диалог с вопросом "Обновить готовность?"
- Slider 0-100 с шагом 5%
- Быстрые кнопки: 25%, 50%, 75%, 100%
- Кнопки "Пропустить" / "Обновить"
- Glass-morphic стиль

#### 11. Интеграция в WorkLogCreateModal (A3) ✅
**Файл:** `modules/modals/components/worklog/WorkLogCreateModal.tsx`
- Загрузка `decomposition_item_progress` для каждой задачи
- После успешного сохранения отчёта → показ ProgressUpdateDialog
- Состояния: `showProgressDialog`, `savedItemInfo`
- Новый handler: `handleProgressDialogClose`

#### 12. Кликабельное название раздела (A4) ✅
**Файл:** `components/timeline/TimelineRow.tsx`
- Название раздела стало `<button>` вместо `<span>`
- Стили: hover:text-primary, hover:underline
- TODO: Открытие SectionModal при клике

#### 13. Прямое редактирование готовности (дополнительно) ✅
**Файл:** `components/timeline/TimelineRow.tsx`
- ProgressCircle в DecompositionItemRow теперь кликабельный
- При клике открывается ProgressUpdateDialog
- Стили при hover: ring, scale
- Тултип: "Готовность: X% — нажмите для изменения"
- После обновления — вызов onProgressUpdated для рефетча данных

#### 14. Optimistic Update для готовности ✅
**Файлы:**
- `hooks/index.ts` — добавлен `useUpdateItemProgress` mutation hook
- `modules/modals/components/progress/ProgressUpdateDialog.tsx` — использует мутацию

**Функционал:**
- Мгновенное обновление UI до ответа сервера (optimistic update)
- Рекурсивный поиск и обновление item в иерархии Project[]
- Автоматический откат при ошибке
- Инвалидация кеша `resourceGraph.all` после успешного обновления

---

## Блок 1: Раздел (Section)

### 1.1 Модалка раздела
**Цель:** Создать модальное окно для просмотра и редактирования раздела

**Что нужно:**
- [ ] Компонент `SectionModal.tsx` в `components/modals/`
- [ ] Режим просмотра: название, даты, ответственный, статус, бюджет
- [ ] Режим редактирования: форма с валидацией (React Hook Form + Zod)
- [ ] Мутация `updateSection` с optimistic update
- [ ] Интеграция: клик по разделу в timeline → открытие модалки

**Поля для отображения:**
- Название раздела
- Даты (начало — конец)
- Ответственный (аватар + имя)
- Статус (chip)
- Плановая готовность (%)
- Фактическая готовность (%)
- Бюджет (план / факт / израсходовано)
- Список этапов декомпозиции

### 1.2 Бюджет на графике раздела
**Цель:** Отобразить бюджетную информацию на таймлайне раздела

**Что нужно:**
- [ ] Компонент `SectionBudgetBar.tsx`
- [ ] Визуализация: прогресс-бар расхода бюджета под периодом раздела
- [ ] Цвет: зелёный (в норме), жёлтый (>80%), красный (>100%)
- [ ] Tooltip: план / факт / % освоения

**Расчёт:**
```
Израсходовано = SUM(work_logs.amount) по всем items раздела
План = SUM(decomposition_items.planned_hours * hourly_rate) или явный budget
```

### 1.3 Убрать статусы разделов
**Цель:** Убрать отображение статуса в строке раздела (status chip)

**Что нужно:**
- [ ] Удалить `StatusChip` из `SectionRow` в `TimelineRow.tsx`
- [ ] Статус остаётся в модалке раздела
- [ ] Убрать статус из sidebar раздела

### 1.4 Показатели при наведении на раздел
**Цель:** При hover на график раздела показывать tooltip со всеми метриками

**Что нужно:**
- [ ] Компонент `SectionMetricsTooltip.tsx`
- [ ] Метрики:
  - План готовность: X%
  - Факт готовность: Y%
  - Израсходованный бюджет: Z ₽ / W ₽ (N%)
- [ ] Trigger: hover на `SectionPeriodFrame` или `ReadinessGraph`

### 1.5 Создание бюджета в разделе
**Цель:** Позволить создавать бюджет для раздела

**Что нужно:**
- [ ] Проверить наличие таблицы `section_budgets` или добавить поле `budget` в `sections`
- [ ] Форма создания бюджета в модалке раздела
- [ ] Мутация `createSectionBudget` / `updateSectionBudget`
- [ ] Валидация: бюджет >= 0

---

## Блок 2: Этап декомпозиции (DecompositionStage)

### 2.1 Рабочий процесс с вехами
**Цель:** Добавить отображение вех (milestones) на таймлайне этапа

**Что нужно:**
- [ ] Проверить наличие таблицы `stage_milestones` или создать
- [ ] Компонент `MilestoneMarker.tsx` — ромб/флаг на дату вехи
- [ ] Tooltip: название вехи, дата, статус
- [ ] Server Action: `getStageMilestones(stageId)`
- [ ] Хук: `useStageMilestones(stageId)`

**Структура вехи:**
```typescript
interface Milestone {
  id: string
  stageId: string
  name: string
  date: string
  status: 'pending' | 'completed' | 'overdue'
}
```

### 2.2 Готовые этапы — серый цвет
**Цель:** Визуально отличать завершённые этапы

**Что нужно:**
- [ ] Определить критерий "готовности" этапа:
  - Статус = "Завершён" ИЛИ
  - Готовность = 100% ИЛИ
  - Все items имеют progress = 100
- [ ] CSS: серый фон / пониженная opacity для бара этапа
- [ ] Применить в `DecompositionStageRow`

**Стиль:**
```css
/* Завершённый этап */
.stage-completed {
  opacity: 0.5;
  filter: grayscale(30%);
}
```

### 2.3 Статусы этапов
**Цель:** Отобразить статус этапа в sidebar

**Что нужно:**
- [ ] Добавить `StatusChip` в `DecompositionStageRow`
- [ ] Статус берётся из `stage.status` (уже есть в типах)
- [ ] Позиция: после названия этапа

### 2.4 Валидация отчётов по срокам этапа
**Цель:** Запретить внесение отчётов вне сроков этапа

**Что нужно:**
- [ ] Валидация на клиенте: проверка `date` отчёта в диапазоне `[stage.startDate, stage.finishDate]`
- [ ] Валидация на сервере: RLS policy или check constraint
- [ ] UI: disabled состояние кнопки "Добавить отчёт" + tooltip с объяснением
- [ ] Сообщение: "Дата отчёта должна быть в пределах сроков этапа (ДД.ММ — ДД.ММ)"

---

## Блок 3: Задача (DecompositionItem)

### 3.1 Изменение процента готовности
**Цель:** Позволить менять progress задачи, запрашивать при внесении отчёта

**Что нужно:**
- [ ] Компонент `ProgressEditor.tsx` — inline редактор или модалка
- [ ] Мутация `updateItemProgress(itemId, progress)`
- [ ] При внесении отчёта (WorkLog):
  - После успешного создания → показать диалог "Обновить готовность?"
  - Предзаполнить текущий progress
  - Кнопки: "Пропустить" / "Обновить"
- [ ] Валидация: 0 <= progress <= 100

**UI Flow:**
```
1. Пользователь вносит отчёт → успех
2. Диалог: "Задача выполнена на X%. Обновить готовность?"
3. Slider или input для нового значения
4. Подтвердить → мутация updateItemProgress
```

### 3.2 График готовности как линия (на этапах)
**Цель:** На графике этапов показать линию фактической готовности

**Что нужно:**
- [ ] Компонент `StageReadinessLine.tsx` — SVG line graph
- [ ] Данные: `stage.actualReadiness[]` (уже есть в типах)
- [ ] Визуализация: линия поверх периода этапа
- [ ] Цвет: синий или цвет статуса
- [ ] Масштаб: высота строки = 100%

**Примечание:** Для этапов пока нет `stage_readiness_snapshots`, нужно:
- Создать таблицу и функцию расчёта (см. README.md "К разработке")
- Или рассчитывать client-side из items

---

## Блок 4: Общее

### 4.1 Внесение отчёта с бюджетами
**Цель:** При внесении отчёта учитывать бюджетную информацию

**Что нужно:**
- [ ] Модалка `WorkLogModal.tsx` (если нет — создать)
- [ ] Поля: дата, часы, описание, (опционально) сумма
- [ ] Расчёт суммы: `hours * employee_hourly_rate`
- [ ] Отображение: текущий бюджет раздела, сколько останется после отчёта
- [ ] Предупреждение: если превышение бюджета

### 4.2 Общая полоса бюджета на таймлайне
**Цель:** Отобразить агрегированный бюджет проекта/стадии на timeline

**Что нужно:**
- [ ] Компонент `BudgetProgressBar.tsx`
- [ ] Уровень: можно на уровне проекта (суммарный) или на уровне раздела
- [ ] Визуализация: горизонтальная полоса под/над периодом
- [ ] Сегменты: освоено / осталось
- [ ] Tooltip: детализация по этапам/разделам

### 4.3 Модалки (полный список)
**Цель:** Создать все необходимые модальные окна

**Список модалок:**
- [ ] `SectionModal.tsx` — просмотр/редактирование раздела
- [ ] `StageModal.tsx` — просмотр/редактирование этапа
- [ ] `ItemModal.tsx` — просмотр/редактирование задачи
- [ ] `WorkLogModal.tsx` — создание/редактирование отчёта
- [ ] `BudgetModal.tsx` — создание/редактирование бюджета
- [ ] `MilestoneModal.tsx` — создание/редактирование вехи
- [ ] `ProgressUpdateDialog.tsx` — быстрое обновление готовности

**Общие требования:**
- React Hook Form + Zod для форм
- Optimistic updates через TanStack Query
- Consistent styling (glass-morphic для dark theme)

### 4.4 Реалтайм
**Цель:** Проверить и обеспечить работу realtime subscriptions

**Чек-лист:**
- [ ] `sections` — изменения отражаются
- [ ] `decomposition_stages` — изменения отражаются
- [ ] `decomposition_items` — изменения отражаются
- [ ] `work_logs` — новые отчёты появляются
- [ ] `loadings` — изменения загрузок
- [ ] `section_readiness_checkpoints` — плановая готовность
- [ ] `section_readiness_snapshots` — фактическая готовность

**Проверить:**
- Подписки в `modules/cache/realtime/config.ts`
- Корректная инвалидация `queryKeys.resourceGraph.all`

### 4.5 Мутации
**Цель:** Проверить все мутации модуля

**Чек-лист:**
- [ ] `updateSection` — обновление раздела
- [ ] `updateDecompositionStage` — обновление этапа
- [ ] `updateDecompositionItem` — обновление задачи
- [ ] `createWorkLog` — создание отчёта
- [ ] `updateWorkLog` — редактирование отчёта
- [ ] `deleteWorkLog` — удаление отчёта
- [ ] `createLoading` — создание загрузки
- [ ] `updateLoading` — редактирование загрузки
- [ ] `deleteLoading` — удаление загрузки
- [ ] `updateItemProgress` — обновление готовности

**Требования:**
- Optimistic updates для UX
- Error handling с rollback
- Toast уведомления

### 4.6 Оптимизация
**Цель:** Проверить производительность модуля

**Чек-лист:**
- [ ] Виртуализация длинных списков (react-window?)
- [ ] Мемоизация компонентов (React.memo)
- [ ] useMemo/useCallback для вычислений
- [ ] Ленивая загрузка данных (по уровням иерархии?)
- [ ] Проверка bundle size
- [ ] Profiling с React DevTools

**Потенциальные проблемы:**
- Много DOM элементов при большом количестве дней/строк
- Тяжёлые SVG расчёты для графиков

### 4.7 Тестирование
**Цель:** Провести полное тестирование модуля

**Сценарии:**

**Отображение:**
- [ ] Иерархия проектов отображается корректно
- [ ] Timeline скроллится синхронно
- [ ] Выходные и праздники подсвечены
- [ ] Фильтры работают

**Разделы:**
- [ ] Модалка открывается/закрывается
- [ ] Редактирование сохраняет данные
- [ ] Бюджет отображается

**Этапы:**
- [ ] Статусы отображаются
- [ ] Завершённые — серые
- [ ] Вехи видны

**Задачи:**
- [ ] Готовность можно изменить
- [ ] График готовности как линия

**Отчёты:**
- [ ] Создание отчёта работает
- [ ] Валидация сроков работает
- [ ] Запрос обновления готовности после отчёта

**Realtime:**
- [ ] Изменения в БД отражаются без перезагрузки
- [ ] Нет дублирования данных

---

## Приоритеты

### Фаза 1: MVP (критично)
1. Модалка раздела (1.1)
2. Изменение готовности задачи (3.1)
3. Внесение отчёта с бюджетами (4.1)
4. Мутации (4.5)

### Фаза 2: Визуализация
1. Бюджет на графике раздела (1.2)
2. Статусы этапов (2.3)
3. Готовые этапы серые (2.2)
4. Показатели при наведении (1.4)

### Фаза 3: Дополнительное
1. Вехи (2.1)
2. Общая полоса бюджета (4.2)
3. Валидация отчётов по срокам (2.4)
4. График готовности как линия (3.2)

### Фаза 4: Качество
1. Убрать статусы разделов (1.3)
2. Создание бюджета (1.5)
3. Все модалки (4.3)
4. Реалтайм проверка (4.4)
5. Оптимизация (4.6)
6. Тестирование (4.7)

---

## Структура файлов (планируемая)

```
modules/resource-graph/
├── components/
│   ├── modals/
│   │   ├── index.ts
│   │   ├── SectionModal.tsx
│   │   ├── StageModal.tsx
│   │   ├── ItemModal.tsx
│   │   ├── WorkLogModal.tsx
│   │   ├── BudgetModal.tsx
│   │   ├── MilestoneModal.tsx
│   │   └── ProgressUpdateDialog.tsx
│   └── timeline/
│       ├── SectionBudgetBar.tsx
│       ├── SectionMetricsTooltip.tsx
│       ├── MilestoneMarker.tsx
│       ├── StageReadinessLine.tsx
│       └── BudgetProgressBar.tsx
├── actions/
│   └── index.ts  # + новые mutations
├── hooks/
│   └── index.ts  # + новые хуки
└── schemas/
    └── index.ts  # Zod schemas для форм
```

---

## Зависимости (потенциальные)

- **Database:**
  - `stage_readiness_snapshots` — для графика готовности этапов
  - `stage_milestones` — для вех
  - `section_budgets` или поле `budget` в `sections`

- **Backend:**
  - Функция `calculate_stage_readiness(stage_id)`
  - Функция `create_daily_stage_readiness_snapshots(date)`
  - pg_cron job для автоматического создания снэпшотов

---

## К обдумыванию

### Отображение отставания от плана

**Статус:** Требует обсуждения, пока не реализуем

**Варианты отставания:**
1. **По готовности** — фактическая готовность < плановой (отстаём от графика)
2. **По бюджету vs готовность** — тратим больше, чем делаем (освоено 77% бюджета, готовность 45%)
3. **По срокам** — текущая дата > плановой даты завершения, готовность < 100%

**Варианты визуализации (ненавязчиво):**
- Красная зона между плановой и фактической линиями
- Маркер отставания на последней точке (например: `45% ⚠️ -15%`)
- Изменение цвета рамки периода при отставании
- Индикатор в sidebar раздела

**Решение:** Отложено до завершения основного функционала

---

## Выполнено

### Бюджет на графике раздела (1.2) ✅
- [x] Миграция: добавлено поле `section_budget_spending` во view `v_resource_graph`
- [x] Realtime: work_logs инвалидирует resourceGraph.all
- [x] Realtime: добавлены подписки на budgets и budget_versions
- [x] Типы: BudgetSpendingPoint, Section.budgetSpending
- [x] transformRowsToHierarchy парсит section_budget_spending
- [x] Компонент BudgetSpendingArea (оранжевая полосатая заливка)
- [x] Интеграция в SectionRow

### Обязательная привязка отчётов к бюджетам ✅
- [x] Миграция: `budget_id` в work_logs теперь NOT NULL
- [x] Удалены work_logs без бюджета (19 записей)
- [x] Триггер валидации: бюджет должен принадлежать тому же разделу
- [x] WorkLogCreateModal: выбор бюджета при создании отчёта
- [x] UI: автовыбор если один бюджет, dropdown если несколько
- [x] Отображение план/потрачено/остаток для выбранного бюджета

---

## Примечания

1. **Бюджеты:** Необходимо уточнить структуру хранения бюджетов (отдельная таблица или поля в существующих)

2. **Hourly Rate:** Для расчёта суммы отчёта нужен `hourly_rate` сотрудника — проверить наличие в данных

3. **Статусы этапов:** Уже есть в типах (`stage.status`), но нужно проверить наполненность данных

4. **Realtime:** После добавления новых таблиц — обновить конфиг подписок

5. **Тёмная тема:** Все новые компоненты должны следовать glass-morphic стилю из README.md
