---
id: "bug-VT-12"
status: "review"
priority: "high"
assignee: "Вадим Тихомиров"
epic: "bug"
dueDate: null
created: "2026-06-16T07:25:00.000Z"
modified: "2026-06-16T07:25:00.000Z"
completedAt: null
labels: ["v1.5.0"]
order: "a12"
---
# bug-VT-12 Шторм Server Actions при загрузке и возврате на вкладку «Разделы»

При тестировании вкладки «Разделы» (Сценарии S1 и S2) обнаружено поведение, полностью аналогичное багу `bug-VT-06` с вкладки «Отделы».

### Ожидаемое поведение (по test-VT-04, S1/S2)
При первой загрузке (S1) должны параллельно уйти запросы контекста, затем 1 раз `getSectionsHierarchy` и 1 раз запрос пользователей. При возврате с другой вкладки (S2) запросов к тяжелым данным вообще быть не должно (`staleTime: Infinity`).

### Фактическое поведение
Сразу после перехода на вкладку и загрузки данных в сеть выстреливает шквал из **19 одинаковых `POST /tasks` запросов** (Server Actions). 
Этот водопад игнорирует кеш (React Query) и повторяется при переключении вкладок. Вероятно, каждый отрендеренный компонент (каждая строка раздела или проекта) независимо вызывает `reloadPermissions` или какой-то другой экшен при монтировании, либо происходит циклический ре-рендер с инвалидацией.

### Как воспроизвести
1. Открыть `/tasks` и авторизоваться.
2. Переключиться на «Разделы» и нажать «Загрузить всё».
3. Переключиться на «Канбан» и вернуться назад.
4. В Network DevTools наблюдается спам из `POST /tasks`.

### Связанные тикеты
- Обнаружено в рамках [test-VT-04](./test-VT-04.md) (Сценарии S1 и S2).
- Аналогично багу [bug-VT-06](./bug-VT-06.md).

---

## Разбор

«19 POST» — это разные Server Actions, а не дубли (как и в bug-VT-06). В Next.js любой Server Action идёт POST'ом на URL текущей страницы (`/tasks`), различаются заголовком `Next-Action`. Гипотеза про `reloadPermissions`/per-row на старте не подтвердилась.

На старте всё свёрнуто → строки разделов (`ObjectSectionRow`) не смонтированы → per-row запросов нет. «19» складываются из:
- глобальный prefetch `<ReferencePrefetch />` (~6, к Разделам не относится, 1 раз за сессию);
- опции фильтра + права (~5): getFilterContext, getOrgStructure, getProjectStructure, getProjectTags;
- `getSectionsHierarchy` (1, тяжёлый);
- календарь (1).

Возврат с другой вкладки — кэш-хит / realtime-эхо (как в bug-VT-07), не систематический рефетч.

## Что сделано

Per-row N+1, который реально был на Разделах, устранён в рамках bug-VT-13 (`useDecompositionStages` теперь `enabled: isExpanded`). Это закрывает и случай, когда на старте смонтированы заранее раскрытые (persist) разделы.

Систематического «шторма дублей» нет.
