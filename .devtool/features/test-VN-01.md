---
id: "test-VN-01"
status: "review"
priority: "high"
assignee: "Владимир Нестерович"
epic: "test"
dueDate: null
created: "2026-06-18T14:30:00.000Z"
modified: "2026-06-19T11:20:00.000Z"
completedAt: null
labels: ["v1.5.0"]
order: "a0"
---
# test-VN-01 Тест модуля «Встречи» (/meetings): функционал, сеть, БД

Тестируем по правилам «Человек + Нейросеть» (как в `test-VT-03`): тестировщик кликает, ИИ ведёт сценарии, читает console/network через Playwright MCP и проверяет данные. На каждый подтверждённый дефект — отдельный тикет `bug-VN-NN` со ссылкой сюда.

## Цель
Полностью прогнать модуль «Встречи»: список созвонов, просмотр протокола, скачивание `.docx`, поиск+подсветка, папки (фильтр/CRUD/назначение/персистентность), шеринг и видимость, адаптив/панели, сайдбар. По каждому действию разобрать сетевые запросы (лишние/дубли) и состояние данных.

## Контекст реализации (для проверяющего ИИ)
- Маршрут: `app/(dashboard)/meetings/page.tsx` → `MeetingsPanel` (модуль `modules/meetings`). 2 панели: список (фильтр папок + поиск) | просмотр протокола.
- **Данные:** `getMeetingReports` (Server Action, идёт как `POST /meetings` с `Next-Action`) читает таблицу `meeting_reports` из **отдельного** Supabase-проекта через service-role (`modules/meetings/server/meetings-client.ts`).
- **Видимость:** созвон виден владельцу (`invited_by_email` = email пользователя; для `fopeto72@gmail.com` — алиас на `vladzimir.nesterovich@enecagroup.com`) **или** тому, кому расшарено. Поле `isOwner` с сервера.
- **Шеринг:** `shareReport`/`unshareReport`/`getReportShares`; таблица `meeting_report_shares` (в meetings-проекте); только владелец.
- **Папки:** локальные (zustand persist, localStorage `meetings-folders`): фильтр (`Все/Без папки/папка`), модалка CRUD, назначение созвона в папку.
- **Протокол:** рендер `report` (jsonb) — summary/участники/обсуждение/вопросы/риски; кнопки скачать `protocol_docx_url`/`transcript_docx_url`.
- **Поиск:** клиентский по теме/участникам/тексту; подсветка в названии и в тексте.
- **Панели:** `react-resizable-panels`, ширины в localStorage (`meetings-panels-reports`). Realtime для meetings отключён.

## Среда
- `localhost:3000`, dev, тестировщик авторизуется сам (залогинен как Владимир Нестерович / `fopeto72@gmail.com`).
- Браузер ведёт ИИ через Playwright MCP; тестировщик кликает/подтверждает.
- ИИ читает console + network через Playwright. **Supabase MCP не подключён** — проверки БД meetings-проекта делаем через SQL, который выполняет тестировщик.

## Сценарии (чек-лист)

### 🔴 Приоритет 1 — список и базовый сетевой профиль
- [x] **S1. Открытие /meetings + загрузка списка.** Открыли — список грузится, фильтр/поиск/счётчик на месте, консоль чистая (только `favicon 404`). ⚠️ ~5 `POST /meetings` на загрузку — см. «Наблюдения».
- [x] **S2. Открытие протокола.** Клик по созвону → корректный рендер: заголовок, мета (17.06.2026 19:27 · Microsoft Teams · Владимир Нестерович), кнопки «Протокол/Транскрипт .docx» (валидные signed-URL), «Поделиться», селект «Папка». Секции: «Кратко», «Участники», «Обсуждение» (тема/статус/итог/ответственный). Пустые «Открытые вопросы»/«Риски» скрыты — верно. ✅ Клик данные не дозапрашивает (из кэша).
- [x] **S3. Скачивание .docx.** ✅ Обе ссылки — валидные signed-URL Supabase Storage (`token`+`exp` ~30 дн), имена `protocol.docx`/`transcript.docx`, `target=_blank`; рендерятся только при наличии URL. (Фактическую отдачу файла кликом не триггерил — открывает новую вкладку к storage; корректность ссылок в нашем коде подтверждена.)
- [ ] **S4. Несформированный протокол.** Созвон с `report=null`/`status≠success` → «Протокол ещё не сформирован». (Если таких нет — N/A.)

### 🔴 Приоритет 1 — поиск
- [x] **S5. Поиск + подсветка.** ✅ «агента» (из тела) → счётчик «Найдено: 1», подсветка `<mark>` в «Кратко» и дважды в «Обсуждении» (в названии нет — не подсвечено, верно). Несуществующее слово → «Найдено: 0» + «Ничего не найдено». Заметка: при пустом результате открытый протокол остаётся в просмотре (норм. UX, не баг).

### 🟡 Приоритет 2 — папки
- [x] **S6. Фильтр по папкам.** ✅ Опции «Все/Без папки/Папка А»; «Без папки» при назначенном созвоне → «Созвонов: 0» + «Созвонов пока нет»; «Папка А» → созвон виден. Счётчик соответствует.
- [x] **S7. Управление папками (CRUD).** ✅ Создание («Тестовая папка», счётчик 0), переименование (→ «Папка А»), счётчик обновляется на «1» после назначения, удаление с подтверждением («Созвоны останутся, но выйдут из папки») → папка исчезла, созвон вернулся, фильтр сброшен на «Все».
- [x] **S8. Назначение в папку + персистентность.** ✅ Селект «Папка созвона» → «Папка А»; F5 → папка, назначение и выбранный фильтр сохранились (localStorage `meetings-folders`): после reload фильтр «Папка А» → созвон на месте.

### 🔴 Приоритет 1 — шеринг
- [x] **S9. Шеринг (выдать/снять).** ✅ «Поделиться» виден у владельца → диалог с поиском по пользователям; клик по пользователю выдаёт доступ сразу (появляется ✓, реальная запись), повторный клик снимает (✓ исчезает). Состояние восстановлено. Сортировку «отмеченные вверх» отдельно не переразглядывал (поиск сужал до 1) — подтверждена кодом.
- [x] **S10. Видимость у получателя.** ✅ Под `fopeto72@gmail.com`: после шеринга владельцем созвон появился («Созвонов: 1»), открывается полный протокол (.docx, секции). До шеринга было пусто (см. SEC2). Кросс-аккаунтная видимость работает.

### 🟢 Приоритет 3 — раскладка/навигация
- [x] **S11. Перетаскиваемые панели.** ✅ Хэндл (`role=separator`) с ограничениями `min 24% / max 55%` (наши `minSize/maxSize`); персистентность round-trip подтверждена: правка localStorage `[34,66]→[45,55]` + F5 → панели применили `[45,55]` (ширины 428/523px). Сам жест drag автоматизировать в Playwright не удалось (panel-group перехватывает pointer на отпускании) — но это поведение библиотеки react-resizable-panels, наша конфигурация (`autoSaveId`, min/max) корректна.
- [x] **S12. Сайдбар «Встречи».** ✅ Пункт «Встречи» (иконка Video) присутствует, ведёт на `/meetings`, на странице подсвечен активным (видно на скрине). Навигация через него работает.

### ⚡ Производительность и оптимизация
- [x] **P1. Сетевой профиль загрузки.** ✅ **Не баг.** На прод-сборке на загрузку — 10 `POST /meetings`, НО это **разные серверные экшены** дашборда (у #79/#80/#50 разные `next-action`-хэши), а не `getMeetingReports` ×10. В Next.js все server actions постятся на текущий URL → экшены лейаута (уведомления/календарь/права/чат) выглядят как `POST /meetings`. #79 = наш `getMeetingReports` (возвращает список корректно). Рефетч-сторма в модуле нет.
- [x] **P2. Кеш-хит при возврате.** ✅ По конфигу: `useMeetingReports` = `createSimpleCacheQuery` со `staleTime: fast` (2 мин) → возврат в пределах окна отдаёт кеш без повторного `getMeetingReports` (гарантировано TanStack Query). Точную сетевую трассировку усложняет «суп» из server-action POST на один URL — но поведение библиотечное.
- [x] **P3. Over-fetch.** ✅ Ответ `getMeetingReports` (прод, #79) содержит `report`, но **не** `transcript` (тяжёлый jsonb исключён) — `REPORT_COLUMNS` работает, `select('*')` нет. Payload компактный.
- [~] **P4. Поиск на объёме.** N/A в этой фазе — у пользователя 1 созвон. Нужен набор побольше, чтобы оценить клиентский поиск/рендер без виртуализации. Отложено.
- [x] **P5. Лишние ре-рендеры/эффекты.** ✅ Косвенно: `getMeetingReports` на загрузку вызывается один раз (см. P1) — значит ре-рендеры от `isLargeScreen`/rehydrate к рефетчу не приводят. Морганий layout на тёплых загрузках не наблюдалось.
- [~] **P6. Широта инвалидации шеринга (наблюдение, не баг).** `share/unshare` инвалидируют `meetings.all` → перезапрашивается весь список созвонов. Для текущего объёма (мало созвонов) приемлемо; при росте — кандидат на точечную инвалидацию (только `meetings.shares`). Оптимизация, не дефект.
- [x] **P7. getReportShares / useUsers.** ✅ Диалог «Поделиться» открывается мгновенно со списком пользователей (`useUsers`, staleTime static — из кеша, не дёргается заново). `getReportShares` подгружает текущие шеры при открытии. Работает корректно.

### 🔒 Безопасность
- [x] **SEC1. Service-role ключ не утекает.** ✅ В Network клиент **не** обращается к meetings-Supabase (`lemntlcygcmzamydfbpg`) напрямую — данные идут через `POST /meetings` (Server Action). Единственное упоминание meetings-проекта на клиенте — временный signed-URL `.docx`. Ключ не `NEXT_PUBLIC`, на клиент не попадает.
- [x] **SEC2. Видимость только своих + расшаренных.** ✅ Под `fopeto72@gmail.com` (не владелец, ничего не расшарено) `/meetings` пуст («Созвонов: 0»). Созвон Владимира не виден. Фильтр видимости (owner OR shared) на сервере работает.
- [x] **SEC3. Шеринг только владельцем.** ✅ У `fopeto72` (получатель, не владелец) в просмотре расшаренного созвона **кнопки «Поделиться» НЕТ** (у владельца была) — управлять доступом может только владелец. Серверный `assertOwner` это дублирует (см. SEC7).
- [x] **SEC4. Auth-гейт.** ✅ На первой навигации без сессии Playwright редиректнул на `/auth/login?next=/meetings` — страница и `getMeetingReports` закрыты для неавторизованных.
- [x] **SEC5. Подписанные .docx.** ✅ Обе ссылки — Supabase Storage signed-URL с `token` и `exp` (≈30 дней), не публичные постоянные.
- [x] **SEC6. XSS в протоколе.** ✅ `report` рендерится как структура — текстовые поля идут текстовыми узлами (через `HighlightedText`), без `dangerouslySetInnerHTML`. HTML/спецсимволы в полях не исполняются (XSS-safe by construction). Целенаправленный ввод вредоносного контента не делал (данные генерит бот).
- [x] **SEC7. IDOR.** ✅ (код+UI) Серверные `shareReport/unshareReport/getReportShares` вызывают `assertOwner` → сверяют `invited_by_email` созвона с email текущего пользователя; у `fopeto72` (≠ владелец) → отказ «Только владелец может управлять доступом». UI не даёт кнопку (SEC3). Forged-вызов экшена напрямую из Playwright воспроизвести нельзя — подтверждено кодом + отсутствием UI-поверхности.

> Часть проверок безопасности/оптимизации ИИ-driven: SEC1 — grep по сборке/Network; SEC2/3/7 — попытки несанкционированного доступа; P1/P3 — Network + прод-сборка; БД — через SQL тестировщика.

## Найденные баги
_(дозаполняется; на каждый подтверждённый баг — отдельный `bug-VN-NN` со ссылкой сюда)_
- _пока нет_

## Наблюдения (не подтверждённые баги)
- **OBS-1 — ЗАКРЫТО (не баг):** «много `POST /meetings` на загрузку» оказалось ложной тревогой. На прод-сборке 10 `POST /meetings` имеют **разные `next-action`-хэши** = это разные серверные экшены всего дашборда (уведомления/календарь/права/чат), которые в Next.js постятся на текущий URL `/meetings`. `getMeetingReports` среди них один (см. P1). Рефетч-сторма в модуле «Встречи» нет. Баг НЕ заводим.
- **OBS-2 (среда, не баг):** в консоли накопились 42 `ERR_CONNECTION_REFUSED` на `POST /meetings` — из окна, когда dev-сервер был недоступен (перезапуск). Сейчас сервер поднят, рендер ок. Связано с OBS-1: при сбое сервера множественные вызовы дают пачку ошибок + retry TanStack Query. Перед дальнейшими проверками — чистая перезагрузка для сброса консоли.
- **OBS-3 (dev-флак, не баг):** дважды (холодный старт + после перелогина) ловил Runtime `SyntaxError: Invalid or unexpected token` / «Application error» на первой загрузке `/meetings` — оба раза **уходило после перезагрузки** (битый чанк во время компиляции dev). На прод-сборке и тёплых загрузках не воспроизводится. Не дефект кода.

## Результаты и отчёт

**Фаза 1 (одиночная, dev, Playwright) — завершена. Багов: 0.**

Пройдено (✅): S1 список, S2 протокол, S3 .docx, S5 поиск+подсветка, S6 фильтр папок, S7 папки CRUD, S8 персистентность папок, S9 шеринг, S11 панели (персистентность), S12 сайдбар; SEC1 ключ не утекает, SEC4 auth-гейт, SEC5 signed-URL, SEC6 XSS-safe. Консоль на тёплых загрузках чистая.

**Фаза 1.5 (прод-сборка) — производительность закрыта.** P1 ✅, P2 ✅, P3 ✅ (нет over-fetch), P5 ✅, P7 ✅; P6 — наблюдение (широкая инвалидация при шеринге, не баг); P4 — N/A (нужен объём созвонов). **OBS-1 закрыто как НЕ баг:** 10 `POST /meetings` на загрузку — это разные серверные экшены всего дашборда (разные `next-action`-хэши), штатно для Next.js; `getMeetingReports` среди них один. OBS-2 — среда (простои сервера).

**Итого багов: 0.** Модуль функционально и по производительности чист.

**Фаза 2 (2 аккаунта: владелец `vladzimir.nesterovich@enecagroup.com` + получатель `fopeto72@gmail.com`) — завершена.**
- SEC2 ✅ — `fopeto72` без шеринга не видит чужой созвон (пусто).
- S10 ✅ — владелец расшарил → у `fopeto72` созвон появился, открывается.
- SEC3 ✅ — у получателя нет кнопки «Поделиться» (управляет только владелец).
- SEC7 ✅ (код+UI) — серверный `assertOwner` блокирует чужой шеринг.

**ИТОГ: все сценарии (S1–S12, SEC1–SEC7, P1–P7) пройдены. Багов: 0.** N/A: S4 (нет пустого протокола), P4 (нужен объём). Наблюдения OBS-1/2/3 — среда/dev, не дефекты.

---

## Для Review (как принято в проекте)
1. **Как протестировано:** прогон через Playwright MCP (ИИ вёл сценарии), функционал/безопасность — в dev, производительность (P1–P3) — на прод-сборке, кросс-аккаунтные сценарии — на двух реальных аккаунтах. Алиас `fopeto72→vladzimir` (временный костыль single-account) убран, т.к. появился настоящий owner-аккаунт.
2. **На что смотреть ревьюеру:**
   - Багов не найдено — модуль «Встречи» функционально, по безопасности и производительности чист.
   - OBS-1 («много POST /meetings») — НЕ баг: это разные server actions дашборда на один URL (проверено по `next-action`-хэшам на проде).
   - P6 — потенциальная микро-оптимизация (шеринг инвалидирует весь список созвонов; при росте объёма — точечнее). Не блокер.
   - Тестовый шеринг созвона на `fopeto72` оставлен в БД (можно снять под владельцем при желании).
