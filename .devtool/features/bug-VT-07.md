---
id: "bug-VT-07"
status: "review"
priority: "high"
assignee: "Вадим Тихомиров"
epic: "bug"
dueDate: null
created: "2026-06-15T13:31:39.000Z"
modified: "2026-06-15T13:31:39.000Z"
completedAt: null
labels: ["v1.5.0"]
order: "a7"
---
# bug-VT-07 Рефетч Server Actions при возврате на вкладку Отделы (отсутствие кеширования)

При переключении с вкладки «Отделы» на другую вкладку (например, «Канбан») и возврате обратно наблюдается повторная отправка Server Actions.

### Ожидаемое поведение (согласно test-VT-03, S2)
Данные отделов (`getDepartmentsData`) имеют настройку `staleTime: Infinity` и не должны перезапрашиваться при возврате на уже посещенную вкладку (должен быть кеш-хит).

### Фактическое поведение
В логах сети зафиксировано два новых `POST http://localhost:3000/tasks` запроса сразу после возврата на вкладку. Это означает, что компоненты заново триггерят Server Actions при монтировании (вероятно, `reloadPermissions` или рефетч `getDepartmentsData` без учета кеша).

### Как воспроизвести
1. На странице `/tasks` перейти на вкладку «Отделы».
2. Дождаться загрузки (см. баг `bug-VT-06`).
3. Переключиться на «Канбан» и сразу вернуться на «Отделы».
4. В Network появится минимум 2 новых POST-запроса к Server Actions.

### Родительская задача
Связано с [test-VT-03](./test-VT-03.md).

---

## Разбор причины

Перепроверено по коду и на localhost. Систематического рефетча тяжёлых данных при возврате на «Отделы» нет: `getDepartmentsData` имеет `staleTime: Infinity` и всегда берётся из кэша. `getTeamsFreshness` имеет `staleTime: 5 минут` (НЕ Infinity) — при возврате в пределах 5 минут это кэш-хит, по истечении — один лёгкий рефетч свежести. Это ожидаемо: freshness намеренно не Infinity, т.к. инвалидируется подтверждением активности команд.

Единственный POST в логе при переключении — `getSectionStatuses` от **Канбана** (к «Отделам» отношения не имеет).

Зафиксированные ранее «2 POST» — разовый эффект realtime-события (эхо своей правки или изменение от другого юзера): широкий префикс `departmentsTimeline.all` инвалидирует список и свежесть разом. Это не сбой кэша, а грубая realtime-инвалидация — относится к bug-VT-09.
