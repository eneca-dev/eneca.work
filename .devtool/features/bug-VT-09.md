---
id: "bug-VT-09"
status: "todo"
priority: "high"
assignee: "Вадим Тихомиров"
epic: "bug"
dueDate: null
created: "2026-06-15T13:36:00.000Z"
modified: "2026-06-15T13:36:00.000Z"
completedAt: null
labels: ["v1.5.0"]
order: "a9"
---
# bug-VT-09 Тяжелый рефетч при изменении данных (drag-to-resize, создание, редактирование)

При выполнении любых мутаций с загрузками на таймлайне (drag-to-resize, создание новой загрузки, редактирование через модалку) происходит отправка лишних Server Actions вместо одного ожидаемого запроса.

### Ожидаемое поведение (test-VT-03, S6)
Должен происходить ровно 1 запрос (Server Action) на сохранение новых дат + должна отрабатывать оптимистичная механика (Optimistic UI) и инвалидация кеша. 

### Фактическое поведение
В ходе тестирования сразу после ресайза или создания загрузки в сеть отправляется по 3 `POST http://localhost:3000/tasks` запроса подряд. 
Это значит, что механизм инвалидации (вероятно `revalidateTag` или `revalidatePath`) работает слишком грубо. После сохранения он заставляет клиент заново скачивать весь массив тяжелых данных (полный рефетч `getDepartmentsData` и других экшенов). Это сильно нагружает сервер и базу при каждом базовом действии (создание, изменение, ресайз).

### Как воспроизвести
1. Открыть `/tasks` -> «Отделы».
2. Изменить загрузку: потянуть за край (ресайз), создать новую (+ Загрузка) или отредактировать существующую.
3. Проверить в Network количество `POST` запросов к `/tasks`. 

### Родительская задача
Связано с [test-VT-03](./test-VT-03.md) (Сценарий S6).
